% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gazer-input.R
\name{prepare_gazepoint_gazer_input}
\alias{prepare_gazepoint_gazer_input}
\title{Prepare Gazepoint data for gazeR}
\usage{
prepare_gazepoint_gazer_input(
  data,
  participant_col = NULL,
  trial_col = NULL,
  time_col = NULL,
  time_unit = c("auto", "seconds", "milliseconds", "samples"),
  sampling_rate_hz = NULL,
  rezero_time = FALSE,
  x_col = NULL,
  y_col = NULL,
  x_left_col = NULL,
  y_left_col = NULL,
  x_right_col = NULL,
  y_right_col = NULL,
  pupil_col = NULL,
  pupil_left_col = NULL,
  pupil_right_col = NULL,
  validity_col = NULL,
  validity_left_col = NULL,
  validity_right_col = NULL,
  valid_values = NULL,
  blink_col = NULL,
  blink_left_col = NULL,
  blink_right_col = NULL,
  invalid_coordinate_values = NULL,
  invalid_pupil_values = NULL,
  mask_invalid = FALSE,
  other_cols = NULL,
  sampling_tolerance = 0.05,
  irregular = c("error", "allow"),
  create_object = FALSE
)
}
\arguments{
\item{data}{Sample-level Gazepoint data frame.}

\item{participant_col}{Participant identifier column. If \code{NULL}, common
participant names are searched.}

\item{trial_col}{Trial identifier column. If \code{NULL}, common trial, media,
and stimulus names are searched.}

\item{time_col}{Numeric time or sample-counter column. If \code{NULL}, common
Gazepoint time columns are searched.}

\item{time_unit}{Source time unit: \code{"auto"}, \code{"seconds"},
\code{"milliseconds"}, or \code{"samples"}. Output time is expressed in
milliseconds.}

\item{sampling_rate_hz}{Sampling rate required when time is represented by
sample indices.}

\item{rezero_time}{Logical. Subtract the minimum time separately within
each participant-trial group.}

\item{x_col, y_col}{Optional monocular or already-combined gaze-coordinate
columns.}

\item{x_left_col, y_left_col}{Optional left-eye gaze-coordinate columns.}

\item{x_right_col, y_right_col}{Optional right-eye gaze-coordinate columns.}

\item{pupil_col}{Optional monocular, cyclopean, or previously combined
pupil column.}

\item{pupil_left_col, pupil_right_col}{Optional left- and right-eye pupil
columns.}

\item{validity_col}{Optional shared gaze/pupil validity column.}

\item{validity_left_col, validity_right_col}{Optional per-eye validity
columns.}

\item{valid_values}{Optional explicit values treated as valid. Without this
argument, positive numeric values, \code{TRUE}, and common textual valid labels
are treated as valid.}

\item{blink_col}{Optional shared blink column.}

\item{blink_left_col, blink_right_col}{Optional per-eye blink columns.}

\item{invalid_coordinate_values}{Optional coordinate values to flag
explicitly as invalid. Zero is not treated as invalid by default because
it can be a valid screen-edge coordinate.}

\item{invalid_pupil_values}{Optional pupil values to flag explicitly as
invalid, for example \code{c(-1, 0)}.}

\item{mask_invalid}{Logical. If \code{TRUE}, explicitly invalid values, failed
validity samples, and blink samples are replaced by \code{NA} in the prepared
gaze and pupil columns. Non-finite values are always represented as \code{NA}.}

\item{other_cols}{Optional item, condition, block, AOI, stimulus, or other
metadata columns retained unchanged.}

\item{sampling_tolerance}{Maximum relative deviation from the median
within-trial sampling interval.}

\item{irregular}{Handling of irregular within-trial sampling:
\code{"error"} or \code{"allow"}.}

\item{create_object}{Logical. If \code{TRUE}, call \code{make_gazer()} from a locally
installed gazeR package. gazeR is GitHub-hosted and is therefore not a
declared gpbiometrics dependency.}
}
\value{
An object of class \code{"gazepoint_gazer_input"} containing:
\itemize{
\item \code{data}: plain gazeR-compatible long-form data;
\item \code{object}: optional output from \code{make_gazer()};
\item \code{row_audit}: row-level availability and invalidity audit;
\item \code{sampling}: participant-trial sampling audit;
\item \code{manifest}: column mappings and preparation summary;
\item \code{settings}: resolved preparation settings.
}
}
\description{
Converts long-form sample-level Gazepoint gaze and pupil data into a
conservative, audited table compatible with \code{gazer::make_gazer()}.
}
\details{
The standardized identifier columns are \code{subject}, \code{trial}, and \code{time}.
\code{time} is expressed in milliseconds.

Monocular or combined input uses the canonical columns \code{x}, \code{y}, and
\code{pupil}. Binocular input uses \code{x_left}, \code{y_left}, \code{pupil_left},
\code{x_right}, \code{y_right}, and \code{pupil_right} as available. gazeR retains
multiple selected eye columns when constructing its compatibility table.

The helper does not assign AOIs, calculate track loss, detect or extend
blinks, interpolate data, smooth signals, downsample, upsample,
baseline-correct pupil size, or run inferential analyses.
}
\examples{
gaze <- data.frame(
  participant = rep("P01", 4),
  trial = rep("T01", 4),
  time_s = c(0, 0.1, 0.2, 0.3),
  gaze_x = c(0.2, 0.4, 0.6, NA),
  gaze_y = c(0.5, 0.5, 0.5, NA),
  pupil_left = c(3.1, 3.2, 3.3, NA),
  pupil_right = c(3.0, 3.1, 3.2, NA)
)

prepared <- prepare_gazepoint_gazer_input(gaze)
prepared$data
prepared$sampling

}
\seealso{
\code{\link[=assign_gazepoint_aoi]{assign_gazepoint_aoi()}},
\code{\link[=downsample_gazepoint_data]{downsample_gazepoint_data()}},
\code{\link[=detect_gazepoint_pupil_blinks]{detect_gazepoint_pupil_blinks()}},
\code{\link[=clean_gazepoint_pupil_signal]{clean_gazepoint_pupil_signal()}}
}
