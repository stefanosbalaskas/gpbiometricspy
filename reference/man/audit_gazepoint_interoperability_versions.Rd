% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interoperability-version-testing.R
\name{audit_gazepoint_interoperability_versions}
\alias{audit_gazepoint_interoperability_versions}
\title{Audit external interoperability versions}
\usage{
audit_gazepoint_interoperability_versions(
  manifest = gazepoint_interoperability_manifest(),
  include_python = TRUE,
  strict = FALSE
)
}
\arguments{
\item{manifest}{Interoperability manifest. Defaults to
\code{\link[=gazepoint_interoperability_manifest]{gazepoint_interoperability_manifest()}}.}

\item{include_python}{Logical. Inspect the active Python environment through
the optional \code{reticulate} package.}

\item{strict}{Logical. Stop when a required contract fails, a declared bridge
function is missing, or an installed dependency is below its minimum
tested version.}
}
\value{
An object of class \code{"gazepoint_interoperability_audit"} containing:
\itemize{
\item \code{results}: target-level compatibility findings;
\item \code{summary}: aggregate pass and review counts;
\item \code{session}: R, Python, operating-system, package, and timestamp metadata;
\item \code{manifest}: the manifest used for the audit.
}
}
\description{
Checks whether declared bridge functions are exported and records the
installed versions of optional R and Python dependencies. Missing optional
dependencies are recorded for review rather than treated as core-package
failures.
}
\details{
Python package versions are queried through \code{importlib.metadata}; target
modules are not imported merely to determine their versions.
}
\examples{
audit <- audit_gazepoint_interoperability_versions(
  include_python = FALSE
)
audit$summary

}
