% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ibi-helpers.R
\name{audit_gazepoint_ibi_quality}
\alias{audit_gazepoint_ibi_quality}
\title{Audit IBI/RR interval quality}
\usage{
audit_gazepoint_ibi_quality(
  data,
  ibi_col = NULL,
  group_cols = NULL,
  time_col = NULL,
  unit = c("auto", "milliseconds", "seconds"),
  min_ibi_ms = 300,
  max_ibi_ms = 2000,
  max_jump_ms = 500
)
}
\arguments{
\item{data}{A data frame.}

\item{ibi_col}{Optional IBI/RR interval column. If \code{NULL}, the function
detects recognised IBI/RR-style column names.}

\item{group_cols}{Optional grouping columns, such as participant, trial,
stimulus, condition, or window labels.}

\item{time_col}{Optional time/order column used to order samples before
successive-difference checks.}

\item{unit}{Unit of the IBI values. \code{"auto"} treats median values below 10 as
seconds and larger values as milliseconds.}

\item{min_ibi_ms}{Minimum plausible IBI in milliseconds.}

\item{max_ibi_ms}{Maximum plausible IBI in milliseconds.}

\item{max_jump_ms}{Maximum plausible absolute change between successive IBI
values within a group.}
}
\value{
A list with \code{overview}, \code{samples}, \code{group_summary}, and \code{settings}.
}
\description{
Audits inter-beat interval (IBI) or RR-interval values in Gazepoint
Biometrics-style exports. This helper deliberately does not use raw \code{HRV}
columns as HRV metrics. HRV-style summaries should be derived only from
genuine IBI/RR interval columns.
}
\examples{
df <- data.frame(
  USER = rep(c("P1", "P2"), each = 4),
  IBI = c(800, 810, 790, 805, 900, 910, 905, 920)
)
audit_gazepoint_ibi_quality(df, group_cols = "USER")

}
