% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-io-cleaning.R
\name{impute_gazepoint_missing}
\alias{impute_gazepoint_missing}
\title{Impute missing values in Gazepoint signals}
\usage{
impute_gazepoint_missing(
  data,
  method = c("linear", "locf", "nocb", "nearest", "constant"),
  cols = NULL,
  time_col = NULL,
  group_cols = NULL,
  max_gap = Inf,
  fill_edges = TRUE,
  constant_value = 0,
  add_flags = TRUE,
  treat_infinite_as_missing = TRUE
)
}
\arguments{
\item{data}{Numeric vector, time-series object, or data frame.}

\item{method}{Imputation method: \code{"linear"}, \code{"locf"}, \code{"nocb"},
\code{"nearest"}, or \code{"constant"}.}

\item{cols}{Columns to impute when \code{data} is a data frame. If NULL, all
numeric columns except time and grouping columns are used.}

\item{time_col}{Optional time column for interpolation.}

\item{group_cols}{Optional grouping columns. Imputation is performed within
groups.}

\item{max_gap}{Maximum missing-gap length, in samples, to impute. Longer
gaps remain missing. Defaults to \code{Inf}.}

\item{fill_edges}{If TRUE, leading and trailing gaps are filled using the
nearest observed value for methods that support it.}

\item{constant_value}{Value used when \code{method = "constant"}.}

\item{add_flags}{If TRUE and \code{data} is a data frame, add logical
\verb{<column>_was_imputed} columns.}

\item{treat_infinite_as_missing}{If TRUE, infinite values are treated as
missing before imputation.}
}
\value{
Object of the same basic type as \code{data}. Data-frame outputs include
an \code{imputation_summary} attribute.
}
\description{
Interpolates missing values in numeric Gazepoint time series, such as pupil,
GSR/EDA, PPG/BVP, heart-rate, IBI/RRI, or other continuous channels. The
function can work on a numeric vector, a time-series object, or selected
numeric columns of a data frame.
}
\examples{
x <- c(1, NA, 3, 4)
impute_gazepoint_missing(x)

dat <- data.frame(time_s = 1:5, GSR = c(1, NA, 3, NA, 5))
impute_gazepoint_missing(dat, cols = "GSR", time_col = "time_s")
}
