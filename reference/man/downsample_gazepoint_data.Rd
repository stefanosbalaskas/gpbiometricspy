% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/downsample.R
\name{downsample_gazepoint_data}
\alias{downsample_gazepoint_data}
\title{Downsample Gazepoint time-series data}
\usage{
downsample_gazepoint_data(
  data,
  time_col,
  signal_cols = NULL,
  group_cols = NULL,
  interval,
  method = c("mean", "median", "first", "last"),
  na_rm = TRUE,
  time_value = c("start", "center", "mean"),
  origin = NULL
)
}
\arguments{
\item{data}{A data frame containing a numeric time column and one or more
numeric signal columns.}

\item{time_col}{Name of the numeric time column.}

\item{signal_cols}{Optional character vector of numeric columns to aggregate.
If \code{NULL}, all numeric columns except \code{time_col} and \code{group_cols} are used.}

\item{group_cols}{Optional character vector of grouping columns. Downsampling
is performed independently within each group.}

\item{interval}{Positive width of each output time bin, expressed in the same
units as \code{time_col}.}

\item{method}{Aggregation method applied to each signal within each bin:
\code{"mean"}, \code{"median"}, \code{"first"}, or \code{"last"}.}

\item{na_rm}{Logical. If \code{TRUE}, missing signal values are removed before
aggregation. If \code{FALSE}, a missing value causes mean or median aggregation
for that signal-bin combination to return \code{NA}.}

\item{time_value}{Value assigned to the output time column: the bin
\code{"start"}, bin \code{"center"}, or mean observed sample time (\code{"mean"}).}

\item{origin}{Optional finite numeric origin used to align the bin grid. If
\code{NULL}, the minimum finite time across the complete input is used.}
}
\value{
A data frame with class \code{"gazepoint_downsampled_data"}. The output
contains grouping columns, the downsampled time column, aggregated signals,
and \code{n_source_rows}. Attributes \code{downsample_log} and
\code{downsample_settings} provide provenance information.
}
\description{
Aggregates selected numeric gaze or biometric signals into fixed-width time
bins. Processing can be performed independently within participant, trial,
session, or other user-defined groups. Only occupied bins are returned; the
function does not fabricate observations for empty periods.
}
\details{
The returned object records the number of contributing source rows for each
bin and stores a structured downsampling log and settings as attributes.
}
\examples{
dat <- data.frame(
  participant = rep(c("P01", "P02"), each = 6),
  time_ms = rep(0:5, 2),
  pupil = c(3.0, 3.1, 3.2, 3.3, 3.4, 3.5,
            2.9, 3.0, 3.1, 3.2, 3.3, 3.4)
)

downsample_gazepoint_data(
  dat,
  time_col = "time_ms",
  signal_cols = "pupil",
  group_cols = "participant",
  interval = 3
)

}
