% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event-physiology-indices.R
\name{epoch_gazepoint_scr}
\alias{epoch_gazepoint_scr}
\title{Epoch Gazepoint SCR/EDA data around events}
\usage{
epoch_gazepoint_scr(
  data,
  events,
  pre,
  post,
  time_col = NULL,
  signal_col = NULL,
  event_time_col = NULL,
  event_id_col = NULL,
  event_group_cols = NULL,
  baseline_window = NULL,
  response_window = NULL,
  min_amplitude = 0.01,
  min_distance_s = 1
)
}
\arguments{
\item{data}{Data frame containing time and EDA/GSR columns.}

\item{events}{Numeric vector of event timestamps or a data frame with an
event-time column.}

\item{pre}{Seconds before each event to include.}

\item{post}{Seconds after each event to include.}

\item{time_col}{Time column in \code{data}. If NULL, common names are detected.}

\item{signal_col}{EDA/GSR signal column. If NULL, common names are detected.}

\item{event_time_col}{Event-time column when \code{events} is a data frame.}

\item{event_id_col}{Optional event identifier column.}

\item{event_group_cols}{Optional event metadata columns to carry into the
output.}

\item{baseline_window}{Baseline window relative to event time. Defaults to
\code{c(-pre, 0)}.}

\item{response_window}{SCR response window relative to event time. Defaults
to \code{c(0, post)}.}

\item{min_amplitude}{Minimum peak-minus-trough amplitude counted as SCR.}

\item{min_distance_s}{Minimum time between counted SCR peaks.}
}
\value{
Data frame with one row per event and event-level SCR metrics.
}
\description{
Segments an EDA/GSR signal around event timestamps and returns event-level
SCR metrics, including SCR count, maximum amplitude, mean amplitude, AUC, and
baseline-corrected epoch summaries.
}
\examples{
eda <- data.frame(time_s = seq(0, 10, by = .1), GSR = sin(seq(0, 10, by = .1)) / 20)
epoch_gazepoint_scr(eda, events = 5, pre = 1, post = 3)
}
