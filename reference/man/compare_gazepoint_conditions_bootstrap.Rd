% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/condition-bootstrap.R
\name{compare_gazepoint_conditions_bootstrap}
\alias{compare_gazepoint_conditions_bootstrap}
\title{Bootstrap condition comparisons for Gazepoint-derived outcomes}
\usage{
compare_gazepoint_conditions_bootstrap(
  data,
  outcome_col,
  condition_col,
  participant_col = NULL,
  condition_levels = NULL,
  paired = FALSE,
  by_cols = NULL,
  statistic = c("mean_difference", "median_difference", "standardized_mean_difference"),
  n_boot = 2000,
  conf_level = 0.95,
  seed = NULL,
  na_rm = TRUE
)
}
\arguments{
\item{data}{Data frame containing the outcome and condition columns.}

\item{outcome_col}{Numeric outcome column.}

\item{condition_col}{Two-level condition column.}

\item{participant_col}{Optional participant/unit identifier. If supplied,
the bootstrap uses participant-by-condition means.}

\item{condition_levels}{Optional two-element character vector defining the
reference and target condition. The estimate is \code{condition_levels[2] - condition_levels[1]}.}

\item{paired}{If TRUE, perform a paired participant-level bootstrap. Requires
\code{participant_col}.}

\item{by_cols}{Optional columns used to run separate comparisons by subgroup.}

\item{statistic}{Statistic to bootstrap: \code{"mean_difference"},
\code{"median_difference"}, or \code{"standardized_mean_difference"}.}

\item{n_boot}{Number of bootstrap resamples.}

\item{conf_level}{Confidence level for percentile intervals.}

\item{seed}{Optional random seed.}

\item{na_rm}{If TRUE, remove rows with missing/non-finite outcomes or missing
condition labels.}
}
\value{
Object of class \code{gazepoint_bootstrap_condition_comparison}, stored as
a data frame. Bootstrap samples are stored in the \code{bootstrap_samples}
attribute.
}
\description{
Compares two conditions using a transparent percentile bootstrap. The helper
is intended for trial-level, participant-level, or event-locked summaries
produced by \code{gpbiometrics}. When \code{participant_col} is supplied, observations
are first averaged at the participant-by-condition level to reduce
pseudo-replication. When \code{paired = TRUE}, only participants with both
conditions are retained and resampled as paired units.
}
