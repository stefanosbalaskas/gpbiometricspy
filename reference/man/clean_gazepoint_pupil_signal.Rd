% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pupil-gaze-cleaning.R
\name{clean_gazepoint_pupil_signal}
\alias{clean_gazepoint_pupil_signal}
\title{Clean Gazepoint pupil signal}
\usage{
clean_gazepoint_pupil_signal(
  data,
  pupil_cols = NULL,
  time_col = NULL,
  group_cols = NULL,
  validity_cols = NULL,
  method = c("linear", "locf", "nocb", "nearest", "constant"),
  max_gap = Inf,
  spike_mad = 6,
  combine = c("all", "any"),
  min_blink_samples = 1L,
  suffix = "_clean",
  keep_flags = TRUE
)
}
\arguments{
\item{data}{Eye-tracking data frame.}

\item{pupil_cols}{Pupil columns. If NULL, common Gazepoint pupil columns are
detected automatically.}

\item{time_col}{Optional time column.}

\item{group_cols}{Optional grouping columns.}

\item{validity_cols}{Optional validity columns corresponding to pupil columns.}

\item{method}{Imputation method passed to \code{impute_gazepoint_missing()}.}

\item{max_gap}{Maximum missing run length, in samples, to interpolate.}

\item{spike_mad}{Robust MAD threshold for pupil outlier spikes.}

\item{combine}{Blink rule passed to \code{detect_gazepoint_pupil_blinks()}.}

\item{min_blink_samples}{Minimum blink run length in samples.}

\item{suffix}{Suffix for cleaned pupil columns.}

\item{keep_flags}{If TRUE, add blink/spike/imputation flag columns.}
}
\value{
Data frame with cleaned pupil columns and a \code{pupil_cleaning_summary}
attribute.
}
\description{
Flags blink samples, non-positive pupil values, and robust outlier spikes,
then interpolates short missing segments. The function returns the original
data with cleaned pupil columns and provenance flags.
}
\examples{
dat <- data.frame(time_s = 0:4, LPD = c(3, NA, 3.2, 40, 3.1))
clean_gazepoint_pupil_signal(dat, pupil_cols = "LPD", time_col = "time_s")
}
