% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beat-correction-audit.R
\name{correct_gazepoint_beats}
\alias{correct_gazepoint_beats}
\title{Apply conservative rule-based IBI corrections}
\usage{
correct_gazepoint_beats(
  audit,
  action = c("mask", "local_median"),
  corrected_col = "ibi_corrected",
  local_window = 5,
  overwrite = FALSE,
  ...
)
}
\arguments{
\item{audit}{A \code{gazepoint_beat_audit} object, or a data frame that can be
passed to \code{audit_gazepoint_beats()}.}

\item{action}{Correction action. One of \code{"mask"} or
\code{"local_median"}.}

\item{corrected_col}{Name of the corrected IBI column to create.}

\item{local_window}{Number of rows on each side to inspect for local-median
replacement when \code{action = "local_median"}.}

\item{overwrite}{Logical. If \code{TRUE}, overwrite an existing corrected
column.}

\item{...}{Arguments passed to \code{audit_gazepoint_beats()} when
\code{audit} is a data frame.}
}
\value{
A list with class \code{gazepoint_beat_correction}, containing
corrected beat data, a correction log, a summary table, and parameters.
}
\description{
Applies a conservative correction action to beats flagged by
\code{audit_gazepoint_beats()}. The default action masks flagged intervals by
setting their corrected IBI to \code{NA}. The local-median action replaces
flagged intervals only when an unflagged local or group median is available.
Every change is logged. The function does not add or remove beat rows and does
not compute or interpret HRV outcomes.
}
