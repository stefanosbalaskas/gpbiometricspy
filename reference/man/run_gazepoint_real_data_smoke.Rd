% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/real-data-smoke-harness.R
\name{run_gazepoint_real_data_smoke}
\alias{run_gazepoint_real_data_smoke}
\title{Run privacy-safe smoke tests on private Gazepoint exports}
\usage{
run_gazepoint_real_data_smoke(
  data_dir = Sys.getenv("GPBIOMETRICS_SMOKE_DIR", unset = ""),
  output_dir = NULL,
  dataset_mode = c("subdirectories", "root"),
  pattern = "\\\\.csv$",
  recursive = TRUE,
  workflow_args = list(),
  diagnostic_args = list(),
  workflow_runner = NULL,
  summary_runner = NULL,
  diagnostic_runner = NULL,
  stop_on_error = FALSE,
  write_results = FALSE,
  overwrite = FALSE,
  protect_repository = TRUE
)
}
\arguments{
\item{data_dir}{Private external data directory. Defaults to
\code{Sys.getenv("GPBIOMETRICS_SMOKE_DIR")}.}

\item{output_dir}{Optional directory for aggregate smoke-test outputs.
This should remain outside the package repository.}

\item{dataset_mode}{How datasets are identified. \code{"subdirectories"} treats
each immediate subdirectory containing matching files as one dataset.
\code{"root"} treats \code{data_dir} as one dataset.}

\item{pattern}{Regular expression used to identify candidate input files.}

\item{recursive}{Search recursively within each dataset directory?}

\item{workflow_args}{Named list of additional arguments passed to
\code{\link[=run_gazepoint_biometrics_workflow]{run_gazepoint_biometrics_workflow()}}.}

\item{diagnostic_args}{Named list of additional arguments passed to
\code{\link[=diagnose_gazepoint_biometrics_workflow]{diagnose_gazepoint_biometrics_workflow()}}.}

\item{workflow_runner}{Optional workflow function. Defaults to
\code{\link[=run_gazepoint_biometrics_workflow]{run_gazepoint_biometrics_workflow()}}. This argument primarily supports
controlled testing.}

\item{summary_runner}{Optional summary function. Defaults to
\code{\link[=summarise_gazepoint_biometrics_workflow]{summarise_gazepoint_biometrics_workflow()}}.}

\item{diagnostic_runner}{Optional diagnostic function. Defaults to
\code{\link[=diagnose_gazepoint_biometrics_workflow]{diagnose_gazepoint_biometrics_workflow()}}.}

\item{stop_on_error}{Stop after the first failed dataset?}

\item{write_results}{Write aggregate CSV outputs to \code{output_dir}?}

\item{overwrite}{Permit replacement of existing aggregate output files?}

\item{protect_repository}{Reject private input or generated output
directories located inside the current package repository?}
}
\value{
An object of class \code{"gazepoint_real_data_smoke"} containing only
privacy-safe aggregate results, condition summaries, runtime metadata,
and non-sensitive settings.
}
\description{
Runs the standard gpbiometrics workflow, workflow summary, and workflow
diagnostic on one or more private Gazepoint export folders. Raw data,
participant-level values, source filenames, and private paths are never
retained in the returned object.
}
\details{
The source directory should normally be outside the package repository and
supplied through the \code{GPBIOMETRICS_SMOKE_DIR} environment variable.
}
\examples{
private_dir <- Sys.getenv("GPBIOMETRICS_SMOKE_DIR")

if (nzchar(private_dir) && dir.exists(private_dir)) {
  smoke <- run_gazepoint_real_data_smoke(
    data_dir = private_dir
  )

  smoke$results
}

}
