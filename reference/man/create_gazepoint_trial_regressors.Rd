% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow-reporting-pipeline.R
\name{create_gazepoint_trial_regressors}
\alias{create_gazepoint_trial_regressors}
\title{Create Gazepoint trial-level regressors for modeling}
\usage{
create_gazepoint_trial_regressors(
  data,
  design,
  pre = 0,
  post = 5,
  time_col = NULL,
  event_time_col = NULL,
  event_id_col = NULL,
  signal_cols = NULL,
  subject_col = NULL,
  design_subject_col = NULL,
  carry_design_cols = NULL
)
}
\arguments{
\item{data}{Data frame containing time-series signals, or a list of data
frames.}

\item{design}{Numeric event timestamps, a design data frame, or a list with
an \code{events} data frame.}

\item{pre}{Seconds before event onset to summarize.}

\item{post}{Seconds after event onset to summarize.}

\item{time_col}{Time column in \code{data}.}

\item{event_time_col}{Event-time column in \code{design}.}

\item{event_id_col}{Trial/event identifier column in \code{design}.}

\item{signal_cols}{Numeric signal columns to summarize. If NULL, all numeric
columns except the time column are used.}

\item{subject_col}{Optional subject/participant column in \code{data}.}

\item{design_subject_col}{Optional subject/participant column in \code{design}.}

\item{carry_design_cols}{Design columns to carry into the output. If NULL,
all non-time design columns are carried.}
}
\value{
A data frame with one row per trial/event and signal summary
regressors.
}
\description{
Prepares a long-format trial table by joining stimulus/event design
information with event-window summaries from numeric Gazepoint channels.
The output is intended for downstream GLM, LMM, or mixed-model workflows.
}
\examples{
dat <- data.frame(time_s = seq(0, 10, by = 1), GSR = seq(0, 1, length.out = 11))
design <- data.frame(trial = "T1", onset = 5, condition = "A")
create_gazepoint_trial_regressors(dat, design, pre = 1, post = 2)
}
