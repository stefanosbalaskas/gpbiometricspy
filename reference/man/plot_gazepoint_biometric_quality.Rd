% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biometric-plots.R
\name{plot_gazepoint_biometric_quality}
\alias{plot_gazepoint_biometric_quality}
\title{Plot Gazepoint biometric quality indicators}
\usage{
plot_gazepoint_biometric_quality(
  data,
  quality_cols = NULL,
  signal_cols = NULL,
  time_col = NULL,
  group_col = NULL,
  dropout_prefix = "biometric_dropout",
  max_points = 5000L,
  main = NULL,
  plot = TRUE,
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{quality_cols}{Optional quality/flag columns. If \code{NULL}, likely quality
columns are detected from names and types.}

\item{signal_cols}{Optional signal columns used to derive missingness flags
when no quality columns are detected.}

\item{time_col}{Optional time/order column recorded in the returned settings.}

\item{group_col}{Optional grouping column for group-level quality summaries.}

\item{dropout_prefix}{Prefix used by dropout columns created by
\code{\link[=flag_gazepoint_biometric_dropouts]{flag_gazepoint_biometric_dropouts()}}.}

\item{max_points}{Maximum number of rows used for row-level returned plot
data.}

\item{main}{Optional plot title.}

\item{plot}{Logical. If \code{FALSE}, no plot is drawn.}

\item{...}{Additional arguments passed to \code{\link[=barplot]{barplot()}}.}
}
\value{
A list with \code{overview}, \code{quality_summary}, \code{group_summary},
\code{plot_data}, and \code{settings}.
}
\description{
Plots and summarises biometric quality indicators such as dropout flags,
validity flags, missingness flags, and quality/audit flags. When no explicit
quality columns are available, the function can derive missingness indicators
from detected biometric signal columns.
}
\examples{
df <- data.frame(
  CNT = 1:5,
  GSR = c(1, NA, 1.2, 1.1, NA),
  HR_valid = c(1, 1, 0, 1, 1)
)
plot_gazepoint_biometric_quality(df, signal_cols = "GSR", plot = FALSE)

}
