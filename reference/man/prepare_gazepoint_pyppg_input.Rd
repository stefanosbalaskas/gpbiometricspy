% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pyppg-hrp-support.R
\name{prepare_gazepoint_pyppg_input}
\alias{prepare_gazepoint_pyppg_input}
\title{Prepare Gazepoint HRP/PPG waveform input for pyPPG}
\usage{
prepare_gazepoint_pyppg_input(
  data,
  ppg_col = NULL,
  time_col = NULL,
  group_cols = NULL,
  sampling_rate = NULL,
  time_unit = c("auto", "ms", "seconds", "samples"),
  min_finite_prop = 0.5,
  output_dir = NULL,
  prefix = "gazepoint_pyppg"
)
}
\arguments{
\item{data}{A Gazepoint biometric data frame or a list containing one.}

\item{ppg_col}{Optional HRP/PPG waveform column. If \code{NULL}, common Gazepoint
HRP/PPG column names are detected.}

\item{time_col}{Optional time, timestamp, or sample-counter column.}

\item{group_cols}{Optional grouping columns.}

\item{sampling_rate}{Optional sampling rate in Hz. Useful when \code{time_col}
is a sample counter such as \code{CNT}.}

\item{time_unit}{Unit of \code{time_col}: \code{"auto"}, \code{"ms"}, \code{"seconds"}, or
\code{"samples"}.}

\item{min_finite_prop}{Minimum finite waveform proportion used for group
readiness summaries.}

\item{output_dir}{Optional directory for CSV export. If \code{NULL}, no files are
written.}

\item{prefix}{File prefix used when \code{output_dir} is supplied.}
}
\value{
A list with \code{overview}, \code{waveform_table}, \code{group_summary},
\code{manifest}, and \code{settings}.
}
\description{
Prepares a Gazepoint heart-rate pulse waveform column, usually \code{HRP}, as a
lightweight input table for optional pyPPG workflows. This helper does not
call Python, does not require pyPPG, and does not derive HRV features. It only
prepares waveform values, timing information when available, and conservative
group-level summaries for interoperability review.
}
