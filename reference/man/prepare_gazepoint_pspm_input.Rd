% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/external-eda-method-bridges.R
\name{prepare_gazepoint_pspm_input}
\alias{prepare_gazepoint_pspm_input}
\title{Prepare Gazepoint EDA input for external PsPM-style workflows}
\usage{
prepare_gazepoint_pspm_input(
  data,
  eda_col = NULL,
  time_col = NULL,
  group_cols = NULL,
  sampling_rate = NULL,
  time_unit = c("auto", "ms", "seconds", "samples"),
  convert_resistance_to_us = FALSE,
  min_finite_prop = 0.5,
  output_dir = NULL,
  prefix = "gazepoint_pspm"
)
}
\arguments{
\item{data}{A Gazepoint biometric data frame, or a list containing one.}

\item{eda_col}{Optional EDA/conductance column. If omitted, the function
prefers \code{GSR_US} when available.}

\item{time_col}{Optional time/counter column.}

\item{group_cols}{Optional grouping columns.}

\item{sampling_rate}{Optional sampling rate in Hz, used when the time column
is a sample counter.}

\item{time_unit}{Unit of \code{time_col}.}

\item{convert_resistance_to_us}{If \code{TRUE}, convert a selected resistance-like
\code{GSR} column to microsiemens as \verb{1,000,000 / GSR}. The default is \code{FALSE}
because this conversion should be used only when the user has verified that
\code{GSR} is resistance-like and \code{GSR_US} is unavailable.}

\item{min_finite_prop}{Minimum finite proportion required for a group to be
labelled ready.}

\item{output_dir}{Optional folder where CSV files should be written.}

\item{prefix}{File prefix used when \code{output_dir} is supplied.}
}
\value{
A list with \code{overview}, \code{signal_table}, \code{group_summary}, \code{manifest},
and \code{settings}.
}
\description{
Prepares a clean Gazepoint EDA/conductance time-series table that can be
exported for external PsPM-style workflows. This function does not run PsPM
and does not attempt to reproduce PsPM internally.
}
