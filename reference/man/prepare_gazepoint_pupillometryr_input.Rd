% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pupillometryr-input.R
\name{prepare_gazepoint_pupillometryr_input}
\alias{prepare_gazepoint_pupillometryr_input}
\title{Prepare Gazepoint pupil data for PupillometryR}
\usage{
prepare_gazepoint_pupillometryr_input(
  data,
  participant_col = NULL,
  trial_col = NULL,
  time_col = NULL,
  condition_col = NULL,
  pupil_left_col = NULL,
  pupil_right_col = NULL,
  pupil_col = NULL,
  time_unit = c("auto", "seconds", "milliseconds", "samples"),
  sampling_rate_hz = NULL,
  rezero_time = FALSE,
  invalid_pupil_values = NULL,
  validity_cols = NULL,
  valid_values = NULL,
  blink_cols = NULL,
  mask_invalid = FALSE,
  create_mean_pupil = TRUE,
  other_cols = NULL,
  sampling_tolerance = 0.05,
  irregular = c("error", "allow"),
  create_object = FALSE
)
}
\arguments{
\item{data}{Sample-level Gazepoint pupil data frame.}

\item{participant_col}{Participant identifier column. If \code{NULL}, common
Gazepoint participant names are searched.}

\item{trial_col}{Trial identifier column. If \code{NULL}, common trial, media,
and stimulus names are searched.}

\item{time_col}{Numeric time or sample-counter column. If \code{NULL}, common
Gazepoint time columns are searched.}

\item{condition_col}{Experimental-condition column. If \code{NULL}, common
condition names are searched.}

\item{pupil_left_col}{Optional left-pupil column.}

\item{pupil_right_col}{Optional right-pupil column.}

\item{pupil_col}{Optional single, cyclopean, or previously averaged pupil
column. Supply either \code{pupil_col} or left/right columns, not both.}

\item{time_unit}{Source time unit: \code{"auto"}, \code{"seconds"},
\code{"milliseconds"}, or \code{"samples"}. The prepared \code{Time} column is expressed
in milliseconds.}

\item{sampling_rate_hz}{Sampling rate required when time is represented by
sample indices.}

\item{rezero_time}{Logical. Subtract the first time separately within each
participant-trial group.}

\item{invalid_pupil_values}{Optional pupil values to flag explicitly as
invalid, for example \code{c(-1, 0)}.}

\item{validity_cols}{Optional pupil-validity columns. Supply one column to
apply it to all pupil channels or one column per pupil channel.}

\item{valid_values}{Optional explicit values treated as valid in
\code{validity_cols}. Without this argument, positive numeric values, \code{TRUE},
and common textual valid labels are treated as valid.}

\item{blink_cols}{Optional blink columns. Supply one column to apply it to
all pupil channels or one column per pupil channel.}

\item{mask_invalid}{Logical. When \code{TRUE}, samples flagged by explicit invalid
values, failed validity, or blink columns are replaced with \code{NA} in the
prepared pupil columns. Non-finite pupil values are always represented as
\code{NA}.}

\item{create_mean_pupil}{Logical. Create \code{Pupil_Mean} when both left and
right pupil columns are available.}

\item{other_cols}{Optional additional condition, item, stimulus, block,
or metadata columns retained unchanged.}

\item{sampling_tolerance}{Maximum relative deviation from the median
within-trial sampling interval.}

\item{irregular}{Handling of irregular sampling: \code{"error"} or \code{"allow"}.}

\item{create_object}{Logical. If \code{TRUE}, construct an actual PupillometryR
object. The optional PupillometryR package must then be installed.}
}
\value{
An object of class \code{"gazepoint_pupillometryr_input"} containing:
\itemize{
\item \code{data}: plain PupillometryR-compatible long-form data;
\item \code{object}: optional PupillometryR object;
\item \code{row_audit}: row-level pupil availability and flag audit;
\item \code{sampling}: participant-trial sampling audit;
\item \code{manifest}: column mappings and preparation summary;
\item \code{settings}: resolved preparation settings.
}
}
\description{
Converts sample-level Gazepoint pupil data into a conservative, audited
long-form table compatible with
\code{PupillometryR::make_pupillometryr_data()}.
}
\details{
The standardized compatibility columns are \code{Subject}, \code{Trial}, \code{Time}, and
\code{Condition}. \code{Time} is expressed in milliseconds. Pupil columns are named
\code{Pupil_Left}, \code{Pupil_Right}, \code{Pupil}, and, when requested and available,
\code{Pupil_Mean}.

The helper does not detect blinks, interpolate gaps, filter pupil signals,
remove trials, baseline-correct pupil size, or run inferential analyses.
Existing blink and validity columns are preserved as audit information and
are used for masking only when \code{mask_invalid = TRUE}.
}
\examples{
pupil <- data.frame(
  participant = rep("P01", 4),
  trial = rep("T01", 4),
  condition = rep("target", 4),
  time_s = c(0, 0.1, 0.2, 0.3),
  pupil_left = c(3.1, 3.2, NA, 3.3),
  pupil_right = c(3.0, 3.1, NA, 3.2)
)

prepared <- prepare_gazepoint_pupillometryr_input(pupil)
prepared$data
prepared$sampling

}
\seealso{
\code{\link[=detect_gazepoint_pupil_blinks]{detect_gazepoint_pupil_blinks()}},
\code{\link[=clean_gazepoint_pupil_signal]{clean_gazepoint_pupil_signal()}},
\code{\link[=baseline_correct_gazepoint_pupil]{baseline_correct_gazepoint_pupil()}}
}
