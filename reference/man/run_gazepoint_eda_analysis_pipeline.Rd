% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda-analysis-pipeline-runner.R
\name{run_gazepoint_eda_analysis_pipeline}
\alias{run_gazepoint_eda_analysis_pipeline}
\title{Run a six-phase Gazepoint EDA/GSR analysis pipeline}
\usage{
run_gazepoint_eda_analysis_pipeline(
  data = NULL,
  path = NULL,
  eda_col = NULL,
  time_col = NULL,
  group_cols = NULL,
  signal_cols = NULL,
  sampling_rate = NULL,
  baseline_window = NULL,
  event_windows = NULL,
  event_data = NULL,
  lag_signal_pair = NULL,
  convert_resistance_to_us = FALSE,
  prepare_external_bridges = TRUE,
  bridge_methods = c("neurokit", "cvxeda", "ledalab", "pspm"),
  prepare_model_data = TRUE,
  create_reports = TRUE,
  output_dir = NULL,
  prefix = "gazepoint_eda_pipeline",
  continue_on_error = TRUE
)
}
\arguments{
\item{data}{Optional Gazepoint biometric data frame or imported object.}

\item{path}{Optional file or folder path. Used only when \code{data} is \code{NULL}.}

\item{eda_col}{Optional EDA/conductance column. If omitted, the runner
prefers \code{GSR_US} when available.}

\item{time_col}{Optional time/counter column.}

\item{group_cols}{Optional grouping columns.}

\item{signal_cols}{Optional biometric signal columns for activity plots and
signal audits.}

\item{sampling_rate}{Optional sampling rate in Hz, used when \code{time_col} is a
sample counter.}

\item{baseline_window}{Optional baseline window object passed to baseline
correction helpers when supported.}

\item{event_windows}{Optional event-window table used for SCR event-window
summaries when available.}

\item{event_data}{Optional event table used for TTL/event alignment when
available.}

\item{lag_signal_pair}{Optional character vector of length two giving signals
for lag estimation.}

\item{convert_resistance_to_us}{Logical. If \code{TRUE}, allow conservative
resistance-to-conductance conversion when the selected EDA column is \code{GSR}.}

\item{prepare_external_bridges}{Logical. If \code{TRUE}, prepare selected external
EDA bridge inputs.}

\item{bridge_methods}{Character vector containing any of \code{"neurokit"},
\code{"cvxeda"}, \code{"ledalab"}, and \code{"pspm"}.}

\item{prepare_model_data}{Logical. If \code{TRUE}, attempt to create SCR hurdle
and biometric LME-ready data objects.}

\item{create_reports}{Logical. If \code{TRUE}, attempt to create report outputs.}

\item{output_dir}{Optional output directory for report bundles or bridge
files where supported.}

\item{prefix}{File prefix used by output-producing helpers where supported.}

\item{continue_on_error}{Logical. If \code{TRUE}, failed steps are stored in
\code{errors} and the pipeline continues.}
}
\value{
A list with \code{overview}, \code{phases}, \code{errors}, \code{pipeline_guide},
\code{model_templates}, \code{reporting_guidance}, \code{interpretation_guardrails}, and
\code{settings}.
}
\description{
Runs a conservative six-phase Gazepoint EDA/GSR workflow using native
\code{gpbiometrics} helpers where possible. The function imports or accepts data,
audits signal quality, prepares preprocessing outputs, creates optional
external-method bridge inputs, prepares synchronization/model-formatting
outputs, attaches model templates, and generates reporting outputs.
}
\details{
The function does not fit \code{brms} or \code{lme4} models, does not run external
software, and does not infer emotion, valence, stress, trust, preference,
cognition, or diagnosis.
}
