% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema-helpers.R
\name{detect_gazepoint_biometric_schema}
\alias{detect_gazepoint_biometric_schema}
\title{Detect the schema of Gazepoint biometric data}
\usage{
detect_gazepoint_biometric_schema(data)
}
\arguments{
\item{data}{A data frame.}
}
\value{
A list with \code{overview}, \code{columns}, \code{time_columns}, \code{timebase},
\code{name_map}, and \code{notes}.
}
\description{
Detects likely biometric, timing, marker, and identifying columns in a
Gazepoint Biometrics export. The function is deliberately descriptive. It
reports what appears to be present and active, but it does not infer emotion,
valence, or HRV from ambiguous raw columns.
}
\examples{
df <- data.frame(
  CNT = 1:5,
  TIME = seq(0, by = 1 / 60, length.out = 5),
  GSR = c(100, 101, 102, 101, 100),
  HR = c(70, 71, 72, 71, 70),
  HRV = c(1, 1, 1, 1, 1)
)
detect_gazepoint_biometric_schema(df)

}
