% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design-event-audit.R
\name{audit_gazepoint_experiment_design}
\alias{audit_gazepoint_experiment_design}
\title{Audit Gazepoint experiment design structure}
\usage{
audit_gazepoint_experiment_design(
  data,
  participant_col,
  trial_col = NULL,
  condition_col = NULL,
  session_col = NULL,
  expected_conditions = NULL,
  min_trials_per_condition = 1L
)
}
\arguments{
\item{data}{A data frame containing trial-, event-, or sample-level records.}

\item{participant_col}{Name of the participant identifier column.}

\item{trial_col}{Optional name of the trial identifier column.}

\item{condition_col}{Optional name of the condition column.}

\item{session_col}{Optional name of the session/block column.}

\item{expected_conditions}{Optional character vector of expected conditions.}

\item{min_trials_per_condition}{Minimum acceptable number of trials per
participant-condition cell when \code{trial_col} and \code{condition_col} are supplied.}
}
\value{
A list with class \code{"gazepoint_experiment_design_audit"}.
}
\description{
Check whether an imported or prepared Gazepoint workflow table contains the
expected participant, trial, condition, and optional session structure before
modelling, event-locking, or time-course analysis.
}
\examples{
dat <- data.frame(
  participant = rep(paste0("P", 1:4), each = 4),
  trial = rep(1:4, times = 4),
  condition = rep(c("A", "B"), times = 8)
)

audit_gazepoint_experiment_design(
  dat,
  participant_col = "participant",
  trial_col = "trial",
  condition_col = "condition",
  expected_conditions = c("A", "B")
)

}
