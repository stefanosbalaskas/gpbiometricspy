% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beat-correction-audit.R
\name{audit_gazepoint_beats}
\alias{audit_gazepoint_beats}
\title{Audit beat or IBI sequences}
\usage{
audit_gazepoint_beats(
  data,
  ibi_col = NULL,
  beat_time_col = NULL,
  group_cols = NULL,
  min_ibi = 300,
  max_ibi = 2000,
  duplicate_tolerance = 0,
  max_relative_change = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{ibi_col}{Optional numeric inter-beat-interval column. If omitted,
intervals are derived from \code{beat_time_col}.}

\item{beat_time_col}{Optional numeric beat-time column. Required when
\code{ibi_col} is omitted. When supplied, rows are ordered by beat time
within each group and duplicate-time checks are enabled.}

\item{group_cols}{Optional character vector of grouping columns, such as
participant, session, trial, or condition.}

\item{min_ibi}{Minimum plausible interval, in the same units as the IBI
column or beat-time column.}

\item{max_ibi}{Maximum plausible interval, in the same units as the IBI
column or beat-time column.}

\item{duplicate_tolerance}{Maximum adjacent beat-time difference treated as a
duplicate-time flag. Ignored when \code{beat_time_col} is \code{NULL}.}

\item{max_relative_change}{Optional relative-change threshold for flagging
abrupt adjacent IBI changes. If \code{NULL}, this check is skipped.}
}
\value{
A list with class \code{gazepoint_beat_audit}, containing beat-level
flags, group summaries, and parameters.
}
\description{
Flags short, long, non-finite, duplicate-time, and abrupt-change intervals in
beat or inter-beat-interval data. The function is intended for quality
control and audit reporting only. It does not remove beats, modify data, or
make physiological, psychological, diagnostic, or clinical claims.
}
