% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eyetrackingr-input.R
\name{prepare_gazepoint_eyetrackingr_input}
\alias{prepare_gazepoint_eyetrackingr_input}
\title{Prepare Gazepoint data for eyetrackingR}
\usage{
prepare_gazepoint_eyetrackingr_input(
  data,
  participant_col = NULL,
  trial_col = NULL,
  time_col = NULL,
  time_unit = c("auto", "seconds", "milliseconds", "samples"),
  sampling_rate_hz = NULL,
  rezero_time = FALSE,
  trackloss_col = NULL,
  validity_col = NULL,
  valid_values = NULL,
  x_col = NULL,
  y_col = NULL,
  aoi_col = NULL,
  aoi_cols = NULL,
  aoi_levels = NULL,
  outside_aoi_values = c("", "none", "no_aoi", "outside", "outside_aoi", "non_aoi",
    "background"),
  allow_aoi_overlap = FALSE,
  item_cols = NULL,
  predictor_cols = NULL,
  treat_non_aoi_looks_as_missing = TRUE,
  sampling_tolerance = 0.05,
  irregular = c("error", "allow"),
  create_object = FALSE
)
}
\arguments{
\item{data}{Sample-level Gazepoint data frame.}

\item{participant_col}{Participant identifier column. If \code{NULL}, common
participant names are searched.}

\item{trial_col}{Trial identifier column. If \code{NULL}, common trial and
stimulus names are searched.}

\item{time_col}{Numeric time or sample-counter column. If \code{NULL}, common
Gazepoint time columns are searched.}

\item{time_unit}{Source time unit: \code{"auto"}, \code{"seconds"},
\code{"milliseconds"}, or \code{"samples"}. Output time is always milliseconds.}

\item{sampling_rate_hz}{Sampling rate required when \code{time_unit = "samples"}
or when an automatically detected sample counter is used.}

\item{rezero_time}{Logical. Subtract the minimum time separately within each
participant-trial group.}

\item{trackloss_col}{Optional column where \code{TRUE} or non-zero means tracking
was lost.}

\item{validity_col}{Optional gaze-validity column where \code{TRUE}, \code{"valid"},
or a positive numeric value means valid tracking.}

\item{valid_values}{Optional explicit values treated as valid in
\code{validity_col}.}

\item{x_col, y_col}{Optional gaze-coordinate columns. Missing or non-finite
coordinates are treated as track loss.}

\item{aoi_col}{Optional categorical AOI column.}

\item{aoi_cols}{Optional existing binary or logical AOI columns. Supply
either \code{aoi_col} or \code{aoi_cols}, not both.}

\item{aoi_levels}{Optional ordered AOI labels to create from \code{aoi_col}.}

\item{outside_aoi_values}{Values in \code{aoi_col} treated as valid looks outside
all supplied AOIs rather than track loss.}

\item{allow_aoi_overlap}{Logical. Permit more than one AOI column to be
\code{TRUE} in a sample.}

\item{item_cols}{Optional item identifier columns retained in the output and
passed to \code{eyetrackingR::make_eyetrackingr_data()}.}

\item{predictor_cols}{Optional condition or predictor columns retained in
the compatibility table.}

\item{treat_non_aoi_looks_as_missing}{Logical passed unchanged to
\code{eyetrackingR::make_eyetrackingr_data()} when \code{create_object = TRUE}.}

\item{sampling_tolerance}{Maximum relative deviation from the median
within-trial sampling interval.}

\item{irregular}{Handling of irregular within-trial sampling:
\code{"error"} or \code{"allow"}.}

\item{create_object}{Logical. If \code{TRUE}, construct an actual
\code{eyetrackingR_data} object. The optional eyetrackingR package must then be
installed.}
}
\value{
An object of class \code{"gazepoint_eyetrackingr_input"} containing:
\itemize{
\item \code{data}: plain compatibility data frame;
\item \code{object}: optional output from \code{make_eyetrackingr_data()};
\item \code{row_audit}: row-level derivation and ordering audit;
\item \code{sampling}: participant-trial sampling audit;
\item \code{manifest}: column mappings and preparation summary;
\item \code{settings}: resolved preparation settings.
}
}
\description{
Converts sample-level Gazepoint gaze and AOI data into a conservative,
audited table compatible with \code{eyetrackingR::make_eyetrackingr_data()}.
}
\details{
The returned compatibility table uses the standardized columns
\code{ParticipantName}, \code{Trial}, \code{Time_ms}, and \code{TrackLoss}. \code{Time_ms} is expressed in
milliseconds. AOI columns are logical.

Track loss is derived conservatively from the union of an explicit
track-loss flag, an invalid validity flag, and missing or non-finite gaze
coordinates. A valid look outside every AOI is not silently reclassified as
hardware track loss.

eyetrackingR is intended for relatively raw sample-level data in which rows
represent equally spaced time samples. Fixation-level or event-level tables
should not be supplied to this helper.
}
\examples{
gaze <- data.frame(
  participant = rep("P01", 4),
  trial = rep("T01", 4),
  time_s = c(0, 0.1, 0.2, 0.3),
  gaze_x = c(0.2, 0.5, 0.8, NA),
  gaze_y = c(0.5, 0.5, 0.5, NA),
  AOI = c("left", "center", "right", NA)
)

prepared <- prepare_gazepoint_eyetrackingr_input(gaze)
prepared$data
prepared$sampling

}
\seealso{
\code{\link[=assign_gazepoint_aoi]{assign_gazepoint_aoi()}}
}
