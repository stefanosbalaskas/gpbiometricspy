% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster-permutation.R
\name{run_gazepoint_cluster_permutation}
\alias{run_gazepoint_cluster_permutation}
\title{Run a conservative cluster-based permutation test for Gazepoint time courses}
\usage{
run_gazepoint_cluster_permutation(
  data,
  outcome_col = "value",
  time_col = "time",
  condition_col = "condition",
  participant_col = "participant",
  design = "within",
  condition_a = NULL,
  condition_b = NULL,
  n_permutations = 1000,
  cluster_forming_alpha = 0.05,
  cluster_alpha = 0.05,
  tail = c("two.sided", "positive", "negative"),
  seed = NULL,
  time_bin_width = NULL,
  aggregation = c("mean", "median")
)
}
\arguments{
\item{data}{A data frame, preferably returned by
\code{\link[=prepare_gazepoint_timecourse_test_data]{prepare_gazepoint_timecourse_test_data()}}.}

\item{outcome_col}{Name of the numeric outcome column.}

\item{time_col}{Name of the numeric time column.}

\item{condition_col}{Name of the condition column.}

\item{participant_col}{Name of the participant identifier column.}

\item{design}{Currently only \code{"within"} is supported.}

\item{condition_a}{Optional first condition level. The tested difference is
\code{condition_a - condition_b}.}

\item{condition_b}{Optional second condition level.}

\item{n_permutations}{Number of sign-flip permutations.}

\item{cluster_forming_alpha}{Per-time-point alpha used only to form clusters.}

\item{cluster_alpha}{Cluster-level alpha used for the \code{significant} flag.}

\item{tail}{Test tail. Currently \code{"two.sided"}, \code{"positive"}, or \code{"negative"}.}

\item{seed}{Optional random seed.}

\item{time_bin_width}{Optional time-bin width passed to
\code{\link[=prepare_gazepoint_timecourse_test_data]{prepare_gazepoint_timecourse_test_data()}} when the input has not already
been prepared.}

\item{aggregation}{Aggregation rule passed to
\code{\link[=prepare_gazepoint_timecourse_test_data]{prepare_gazepoint_timecourse_test_data()}} when needed.}
}
\value{
An object of class \code{gazepoint_cluster_permutation}.
}
\description{
Runs a narrow within-subject, two-condition, one-dimensional cluster-based
permutation test on participant-level Gazepoint-derived time courses.
}
\details{
The function computes a paired t-statistic at each time point using
participant-level condition differences, forms temporal clusters from
adjacent suprathreshold time points, uses summed absolute t-statistics as
cluster mass, and compares observed cluster masses with a sign-flip
permutation null distribution.
}
\section{Caution}{

This helper tests the global null of no condition difference anywhere in the
tested time range. A significant cluster indicates evidence against that
global null under the permutation scheme. It does not establish the precise
onset, offset, latency, or physiological timing of an effect. Avoid wording
such as "the effect starts at X ms". Prefer conservative wording such as
"the cluster-based permutation test indicated a condition difference in the
tested time course".
}

