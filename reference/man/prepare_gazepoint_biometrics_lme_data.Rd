% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biometrics-lme-data.R
\name{prepare_gazepoint_biometrics_lme_data}
\alias{prepare_gazepoint_biometrics_lme_data}
\title{Prepare Gazepoint biometric summaries for mixed-model analysis}
\usage{
prepare_gazepoint_biometrics_lme_data(
  data,
  outcome_col,
  fixed_effect_cols = NULL,
  condition_cols = NULL,
  covariate_cols = NULL,
  random_effect_cols = NULL,
  participant_col = NULL,
  stimulus_col = NULL,
  trial_col = NULL,
  window_col = NULL,
  baseline_col = NULL,
  baseline_correct = FALSE,
  factor_cols = NULL,
  continuous_cols = NULL,
  scale_continuous = FALSE,
  include_window = TRUE,
  drop_missing = TRUE,
  min_rows = 10
)
}
\arguments{
\item{data}{A data frame containing biometric summary rows.}

\item{outcome_col}{Name of the outcome column to analyse.}

\item{fixed_effect_cols}{Optional fixed-effect predictor columns.}

\item{condition_cols}{Optional condition/design columns to include as fixed
effects.}

\item{covariate_cols}{Optional covariate columns to include as fixed effects.}

\item{random_effect_cols}{Optional grouping columns for random intercepts.}

\item{participant_col, stimulus_col, trial_col}{Optional common grouping columns.}

\item{window_col}{Optional analysis-window column. Included as a fixed effect
when \code{include_window = TRUE}.}

\item{baseline_col}{Optional baseline column.}

\item{baseline_correct}{Logical. If \code{TRUE}, creates an outcome column equal to
\code{outcome_col - baseline_col}.}

\item{factor_cols}{Optional columns to convert to factors.}

\item{continuous_cols}{Optional numeric predictor columns to scale when
\code{scale_continuous = TRUE}.}

\item{scale_continuous}{Logical. If \code{TRUE}, creates z-scored versions of
numeric continuous predictors and uses those in the formula.}

\item{include_window}{Logical. Should \code{window_col} be included as a fixed
effect?}

\item{drop_missing}{Logical. Should incomplete model rows be removed from
\code{model_data}?}

\item{min_rows}{Minimum number of complete rows required for a \code{"ready"}
status.}
}
\value{
A list with \code{overview}, \code{data}, \code{model_data}, \code{model_formula},
\code{variable_summary}, and \code{settings}.
}
\description{
Prepares biometric window-level or event-level summaries for downstream
mixed-model analysis. This helper does not fit a model. It checks variables,
optionally baseline-corrects the selected outcome, optionally scales numeric
predictors, converts grouping/factor variables, flags complete cases, and
returns a conservative model formula.
}
