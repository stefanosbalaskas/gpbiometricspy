% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bids-layout-audit.R
\name{check_gazepoint_bids}
\alias{check_gazepoint_bids}
\title{Check a Gazepoint dataset for a BIDS-like layout}
\usage{
check_gazepoint_bids(
  root,
  subject_pattern = "^sub-[A-Za-z0-9]+$",
  recursive = TRUE,
  expected_files = c("dataset_description.json", "participants.tsv"),
  gazepoint_patterns = c("all[_-]?gaze", "fixation", "summary", "biometric", "eda",
    "gsr", "ecg", "ppg", "hr", "ibi")
)
}
\arguments{
\item{root}{Character scalar. Path to the dataset root folder.}

\item{subject_pattern}{Regular expression used to identify subject folders.
Defaults to folders such as \code{sub-001}.}

\item{recursive}{Logical. Whether to search for Gazepoint export files
recursively under \code{root}.}

\item{expected_files}{Character vector of optional dataset-level files to check.}

\item{gazepoint_patterns}{Character vector of case-insensitive filename
patterns used to identify Gazepoint-derived exports.}
}
\value{
A list with class \code{gazepoint_bids_layout_audit} containing the root
path, check table, discovered files, and summary counts.
}
\description{
Performs a conservative, Gazepoint-oriented audit of a dataset folder that
follows, or approximately follows, a BIDS-like organization. This function is
not a full BIDS validator and does not convert data. It checks only simple,
reviewable layout features such as dataset-level metadata files, subject
folders, Gazepoint-like export files, and optional JSON sidecars.
}
