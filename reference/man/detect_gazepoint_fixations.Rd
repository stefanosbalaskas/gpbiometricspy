% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gaze-event-detection.R
\name{detect_gazepoint_fixations}
\alias{detect_gazepoint_fixations}
\title{Detect fixations and saccades in Gazepoint gaze samples}
\usage{
detect_gazepoint_fixations(
  data,
  time_col = NULL,
  x_col = NULL,
  y_col = NULL,
  group_cols = NULL,
  valid_col = NULL,
  valid_values = c(1, TRUE),
  time_unit = c("seconds", "milliseconds", "microseconds", "samples"),
  sampling_rate_hz = NULL,
  coordinate_unit = c("native", "normalized", "pixels", "degrees"),
  velocity_threshold,
  min_fixation_duration_ms = 100,
  min_saccade_duration_ms = 10,
  max_gap_ms = 100,
  velocity_col = "gaze_velocity",
  class_col = "gaze_class",
  event_id_col = "gaze_event_id",
  overwrite = FALSE
)
}
\arguments{
\item{data}{A data frame containing sample-level gaze coordinates.}

\item{time_col}{Numeric time column. If \code{NULL}, common Gazepoint time-column
names are searched.}

\item{x_col}{Numeric horizontal gaze-coordinate column. If \code{NULL}, common
Gazepoint names such as \code{BPOGX}, \code{gaze_x}, and \code{FPOGX} are searched.}

\item{y_col}{Numeric vertical gaze-coordinate column. If \code{NULL}, common
Gazepoint names such as \code{BPOGY}, \code{gaze_y}, and \code{FPOGY} are searched.}

\item{group_cols}{Optional participant, trial, session, or file columns.
Detection is performed independently within each group.}

\item{valid_col}{Optional gaze-validity column.}

\item{valid_values}{Values in \code{valid_col} treated as valid.}

\item{time_unit}{Unit of \code{time_col}: \code{"seconds"}, \code{"milliseconds"},
\code{"microseconds"}, or \code{"samples"}.}

\item{sampling_rate_hz}{Sampling rate required when
\code{time_unit = "samples"}.}

\item{coordinate_unit}{Descriptive unit of the gaze coordinates:
\code{"native"}, \code{"normalized"}, \code{"pixels"}, or \code{"degrees"}. Coordinates are
not transformed automatically.}

\item{velocity_threshold}{Positive velocity threshold expressed in coordinate
units per second. Samples above this threshold are classified as saccadic.}

\item{min_fixation_duration_ms}{Minimum fixation duration in milliseconds.
Shorter fixation runs are labelled \code{"unclassified"}.}

\item{min_saccade_duration_ms}{Minimum saccade duration in milliseconds.
Shorter saccade runs are labelled \code{"unclassified"}.}

\item{max_gap_ms}{Maximum permitted time difference between adjacent samples.
Larger gaps split events. Set to \code{NULL} to disable this check.}

\item{velocity_col}{Name of the generated sample-velocity column.}

\item{class_col}{Name of the generated sample-classification column.}

\item{event_id_col}{Name of the generated sample event-ID column.}

\item{overwrite}{Logical. If \code{FALSE}, existing generated columns are
protected.}
}
\value{
An object of class \code{"gazepoint_gaze_events"} containing:
\itemize{
\item \code{samples}: original rows with velocity, class, and event ID;
\item \code{fixations}: fixation-level timing, location, and dispersion summaries;
\item \code{saccades}: saccade-level timing, amplitude, direction, and velocity;
\item \code{summary}: group-level sample and event counts;
\item \code{settings}: complete detector settings.
}
}
\description{
Applies a transparent velocity-threshold classification to raw Gazepoint
gaze coordinates. Samples are classified as fixation, saccade, unclassified,
or invalid, after which fixation- and saccade-level event tables are derived.
}
\details{
This is an I-VT-style detector rather than an exact reproduction of a
specific vendor or external-toolbox implementation. The velocity threshold
must match the supplied coordinate unit and should be justified, reported,
and sensitivity-checked for the recording design.
}
\examples{
gaze <- data.frame(
  time_s = seq(0, 0.9, by = 0.1),
  gaze_x = c(0, 0.01, 0.02, 0.03, 1, 1.01, 1.02, 1.03, 1.04, 1.05),
  gaze_y = 0
)

events <- detect_gazepoint_fixations(
  gaze,
  time_col = "time_s",
  x_col = "gaze_x",
  y_col = "gaze_y",
  velocity_threshold = 2,
  min_fixation_duration_ms = 100,
  min_saccade_duration_ms = 50
)

events$fixations
events$saccades

}
\references{
Salvucci, D. D., and Goldberg, J. H. (2000). Identifying fixations and
saccades in eye-tracking protocols. Proceedings of the Eye Tracking Research
and Applications Symposium.
}
\seealso{
\code{\link[=detect_gazepoint_saccades]{detect_gazepoint_saccades()}},
\code{\link[=plot_gazepoint_saccade_main_sequence]{plot_gazepoint_saccade_main_sequence()}},
\code{\link[=summarize_gazepoint_fixations]{summarize_gazepoint_fixations()}}
}
