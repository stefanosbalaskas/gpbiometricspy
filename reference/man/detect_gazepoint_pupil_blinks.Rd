% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pupil-gaze-cleaning.R
\name{detect_gazepoint_pupil_blinks}
\alias{detect_gazepoint_pupil_blinks}
\title{Detect blink intervals from Gazepoint pupil data}
\usage{
detect_gazepoint_pupil_blinks(
  data,
  pupil_cols = NULL,
  time_col = NULL,
  group_cols = NULL,
  validity_cols = NULL,
  invalid_values = c(0),
  nonpositive_is_missing = TRUE,
  combine = c("all", "any"),
  min_blink_samples = 1L,
  return = c("intervals", "onsets", "flags")
)
}
\arguments{
\item{data}{Eye-tracking data frame.}

\item{pupil_cols}{Pupil columns. If NULL, common Gazepoint names such as
\code{LPD} and \code{RPD} are detected automatically.}

\item{time_col}{Time column. If NULL, common Gazepoint time columns are
detected automatically.}

\item{group_cols}{Optional grouping columns, such as participant or trial.}

\item{validity_cols}{Optional validity columns corresponding to
\code{pupil_cols}, such as \code{LPV} and \code{RPV}.}

\item{invalid_values}{Numeric values treated as missing pupil samples.}

\item{nonpositive_is_missing}{If TRUE, pupil values less than or equal to
zero are treated as invalid.}

\item{combine}{Whether a blink requires \code{"all"} or \code{"any"} selected pupil
channels to be invalid.}

\item{min_blink_samples}{Minimum run length in samples.}

\item{return}{Return \code{"intervals"}, \code{"onsets"}, or \code{"flags"}.}
}
\value{
A data frame of blink intervals by default. If \code{return = "onsets"},
a numeric vector of blink onset times is returned. If \code{return = "flags"},
a logical vector marking blink samples is returned.
}
\description{
Identifies blink-like intervals as runs of missing, zero, non-finite, or
invalid pupil samples. For binocular data, the default treats a blink as a
sample where all selected pupil channels are invalid.
}
\examples{
dat <- data.frame(time_s = 0:4, LPD = c(3, NA, NA, 3.1, 3.2))
detect_gazepoint_pupil_blinks(dat, pupil_cols = "LPD", time_col = "time_s")
}
