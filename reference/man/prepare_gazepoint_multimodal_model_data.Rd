% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_data.R
\name{prepare_gazepoint_multimodal_model_data}
\alias{prepare_gazepoint_multimodal_model_data}
\title{Prepare Gazepoint multimodal model data}
\usage{
prepare_gazepoint_multimodal_model_data(
  biometrics,
  eye_tracking = NULL,
  group_columns = NULL,
  biometric_is_summarised = FALSE,
  by = NULL,
  all = FALSE
)
}
\arguments{
\item{biometrics}{A data frame containing row-level Gazepoint Biometrics data
or an already summarised biometric window table.}

\item{eye_tracking}{Optional eye-tracking summary table to merge with the
biometric summaries.}

\item{group_columns}{Columns defining the analysis unit, such as
\code{c("USER", "MEDIA_ID")}.}

\item{biometric_is_summarised}{Logical. If \code{FALSE}, biometric window
summaries are created using \code{\link[=summarise_gazepoint_multimodal_windows]{summarise_gazepoint_multimodal_windows()}}.
If \code{TRUE}, \code{biometrics} is treated as already summarised.}

\item{by}{Optional merge keys. If \code{NULL}, \code{group_columns} are used.}

\item{all}{Should a full outer join be used when eye-tracking data are
supplied? Defaults to \code{FALSE}, giving an inner join.}
}
\value{
A data frame with class \code{"gazepoint_multimodal_model_data"} and a
\code{"model_data_summary"} attribute.
}
\description{
Creates a model-ready table from Gazepoint biometric window summaries and,
optionally, eye-tracking summaries produced by \code{gp3tools} or another
workflow. The function is intentionally conservative: it does not fit a
model, impute missing values, or remove rows automatically.
}
