% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-folder-profile.R
\name{profile_gazepoint_export_folder}
\alias{profile_gazepoint_export_folder}
\title{Profile a Gazepoint export folder before analysis}
\usage{
profile_gazepoint_export_folder(
  path,
  pattern = "\\\\.csv$",
  recursive = FALSE,
  max_files = Inf,
  max_rows = Inf,
  na.strings = c("", "NA", "NaN")
)
}
\arguments{
\item{path}{Path to a folder containing Gazepoint-style export files.}

\item{pattern}{File-name regular expression. Defaults to CSV files.}

\item{recursive}{Logical. If \code{TRUE}, search subfolders recursively.}

\item{max_files}{Maximum number of matching files to inspect.}

\item{max_rows}{Maximum number of rows to read per file for profiling.
Use \code{Inf} to read complete files.}

\item{na.strings}{Character vector of strings to treat as missing values.}
}
\value{
A list with class \code{"gazepoint_export_folder_profile"} containing
\code{overview}, \code{files}, \code{columns}, \code{warnings}, and \code{settings} tables.
}
\description{
Create a compact, auditable profile of a Gazepoint-style export folder
before running a full workflow. The profiler inspects files, row and column
counts, likely Gazepoint column roles, missingness, numeric signal activity,
constant columns, all-zero columns, and read errors.
}
\examples{
demo_dir <- system.file(
  "extdata",
  "gazepoint_biometrics_kiosk_demo_exports",
  package = "gpbiometrics"
)

if (nzchar(demo_dir)) {
  profile <- profile_gazepoint_export_folder(demo_dir, max_files = 2)
  profile
}

}
