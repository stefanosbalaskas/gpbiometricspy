% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-io-cleaning.R
\name{import_gazepoint_data}
\alias{import_gazepoint_data}
\title{Import Gazepoint export files from a session folder}
\usage{
import_gazepoint_data(
  dir,
  session = NULL,
  pattern = "\\\\.csv$",
  recursive = FALSE,
  session_match = c("prefix", "contains", "regex"),
  file_encoding = "UTF-8-BOM",
  add_file_info = TRUE
)
}
\arguments{
\item{dir}{Folder containing Gazepoint export files.}

\item{session}{Optional session or participant prefix used to select files.
Can be a character vector.}

\item{pattern}{File pattern. Defaults to CSV files.}

\item{recursive}{If TRUE, search subfolders.}

\item{session_match}{How \code{session} should be matched: \code{"prefix"},
\code{"contains"}, or \code{"regex"}.}

\item{file_encoding}{Encoding passed to \code{read.table()}.}

\item{add_file_info}{If TRUE, add source-file columns to each imported data
frame.}
}
\value{
A named list of data frames with class \code{gazepoint_session_data}.
File metadata are stored in the \code{file_index} attribute.
}
\description{
Loads Gazepoint-style CSV exports from a folder into a named list of data
frames. The function is intended as a single entry point for common session
folders that contain all-gaze, fixation, summary, biometric, marker, or
related exports.
}
\examples{
\dontrun{
session_data <- import_gazepoint_data("path/to/session", session = "P01")
names(session_data)
attr(session_data, "file_index")
}
}
