% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biosppy-input.R
\name{prepare_gazepoint_biosppy_input}
\alias{prepare_gazepoint_biosppy_input}
\title{Prepare Gazepoint signals for Python BioSPPy}
\usage{
prepare_gazepoint_biosppy_input(
  data,
  signal_type = c("auto", "eda", "ppg"),
  signal_col = NULL,
  time_col = NULL,
  group_cols = NULL,
  sampling_rate_hz = NULL,
  missing = c("error", "interpolate", "segments"),
  irregular = c("error", "allow"),
  sampling_tolerance = 0.05,
  min_segment_samples = 3L,
  signal_units = NULL,
  output_dir = NULL,
  prefix = "gazepoint_biosppy",
  write_manifest = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{data}{A numeric signal vector or a data frame containing EDA/GSR or
PPG/BVP samples.}

\item{signal_type}{Signal type: \code{"auto"}, \code{"eda"}, or \code{"ppg"}. Numeric-vector
input requires an explicit signal type.}

\item{signal_col}{Signal column when \code{data} is a data frame. If \code{NULL},
common Gazepoint and biosignal column names are searched.}

\item{time_col}{Optional numeric time column expressed in seconds. If absent,
\code{sampling_rate_hz} must be supplied and a time vector is generated.}

\item{group_cols}{Optional participant, session, trial, file, or other
grouping columns.}

\item{sampling_rate_hz}{Optional positive sampling frequency in hertz. When
omitted, it is inferred separately for each group from \code{time_col}.}

\item{missing}{Missing-signal handling: \code{"error"}, \code{"interpolate"}, or
\code{"segments"}.}

\item{irregular}{Handling of irregular sampling intervals: \code{"error"} or
\code{"allow"}.}

\item{sampling_tolerance}{Maximum relative deviation from the expected sample
interval before an interval is marked irregular.}

\item{min_segment_samples}{Minimum number of samples retained when
\code{missing = "segments"}.}

\item{signal_units}{Optional descriptive signal unit, such as
\code{"microsiemens"} or \code{"arbitrary"}.}

\item{output_dir}{Optional directory for one-column CSV files and a manifest.}

\item{prefix}{Filename prefix used for exported files.}

\item{write_manifest}{Logical. Write a manifest when \code{output_dir} is supplied.}

\item{overwrite}{Logical. Permit replacement of existing output files.}
}
\value{
An object of class \code{"gazepoint_biosppy_input"} containing:
\itemize{
\item \code{samples}: row-level audit table;
\item \code{vectors}: named Python-ready numeric signal vectors;
\item \code{sampling_rates_hz}: sampling frequency for each vector;
\item \code{manifest}: vector-level preparation summary;
\item \code{files}: paths written to disk;
\item \code{settings}: preparation settings and Python call templates.
}
}
\description{
Converts Gazepoint EDA/GSR or PPG/BVP waveform data into grouped numeric
vectors suitable for transfer to Python BioSPPy. The function validates the
timebase, records missing-data handling, and can write headerless one-column
CSV files.
}
\details{
The function prepares data only. It does not invoke Python or BioSPPy and
does not perform physiological interpretation.

BioSPPy signal functions assume a regularly sampled one-dimensional signal
and a sampling frequency in hertz. When a time column is supplied, each
group must be strictly increasing. Sampling irregularity is assessed against
the supplied or inferred sampling rate.

With \code{missing = "interpolate"}, non-finite signal values are replaced by
linear interpolation within each group. Edge values use the nearest finite
value. At least two finite samples are required.

With \code{missing = "segments"}, each contiguous finite run is exported as a
separate vector. Runs shorter than \code{min_segment_samples} are retained in the
audit table but excluded from the prepared vectors.

Exported signal files contain one numeric value per line without a header,
quotation marks, or row names.
}
\examples{
eda <- data.frame(
  participant = rep("P01", 4),
  time_s = c(0, 0.1, 0.2, 0.3),
  EDA = c(1.0, 1.1, 1.05, 1.2)
)

prepared <- prepare_gazepoint_biosppy_input(
  eda,
  signal_type = "eda",
  group_cols = "participant"
)

prepared$vectors$P01
prepared$sampling_rates_hz

}
\seealso{
\code{\link[=run_gazepoint_biosppy_eda]{run_gazepoint_biosppy_eda()}},
\code{\link[=run_gazepoint_biosppy_ppg]{run_gazepoint_biosppy_ppg()}}
}
