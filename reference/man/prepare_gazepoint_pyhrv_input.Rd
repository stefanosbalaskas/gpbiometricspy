% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pyhrv-input.R
\name{prepare_gazepoint_pyhrv_input}
\alias{prepare_gazepoint_pyhrv_input}
\title{Prepare Gazepoint intervals for Python pyHRV}
\usage{
prepare_gazepoint_pyhrv_input(
  data,
  ibi_col = NULL,
  group_cols = NULL,
  unit = c("auto", "milliseconds", "seconds"),
  filter = c("none", "plausible"),
  min_nni_ms = 300,
  max_nni_ms = 2000,
  collapse_repeated_intervals = FALSE,
  repeated_tolerance_ms = 1e-08,
  output_dir = NULL,
  prefix = "gazepoint_pyhrv",
  write_manifest = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{data}{A numeric interval vector or a data frame containing an IBI,
RR, or NN-interval column.}

\item{ibi_col}{Interval column when \code{data} is a data frame. If \code{NULL},
common Gazepoint, RR, and NNI column names are searched.}

\item{group_cols}{Optional participant, session, trial, or file columns.
One pyHRV-ready vector is produced per group.}

\item{unit}{Input interval unit: \code{"auto"}, \code{"milliseconds"}, or
\code{"seconds"}.}

\item{filter}{Which intervals should be included in the pyHRV vectors:
\code{"none"} retains all finite positive intervals, whereas \code{"plausible"}
also applies \code{min_nni_ms} and \code{max_nni_ms}.}

\item{min_nni_ms}{Minimum plausible NN interval in milliseconds.}

\item{max_nni_ms}{Maximum plausible NN interval in milliseconds.}

\item{collapse_repeated_intervals}{Logical. If \code{TRUE}, consecutive interval
values equal within \code{repeated_tolerance_ms} are represented once. This can
be useful for sample-level Gazepoint exports in which the current IBI is
repeated across multiple rows.}

\item{repeated_tolerance_ms}{Non-negative tolerance used to identify
consecutive repeated intervals.}

\item{output_dir}{Optional directory in which one-column CSV files and a
manifest are written.}

\item{prefix}{Filename prefix used when \code{output_dir} is supplied.}

\item{write_manifest}{Logical. If \code{TRUE} and \code{output_dir} is supplied,
write a group-level manifest CSV.}

\item{overwrite}{Logical. If \code{FALSE}, existing output files are protected.}
}
\value{
An object of class \code{"gazepoint_pyhrv_input"} containing:
\itemize{
\item \code{intervals}: auditable row-level interval table;
\item \code{vectors}: named list of pyHRV-ready numeric millisecond vectors;
\item \code{manifest}: group-level interval and exclusion summary;
\item \code{files}: paths written when \code{output_dir} is supplied;
\item \code{settings}: complete preparation settings.
}
}
\description{
Converts Gazepoint inter-beat or RR intervals into millisecond NN-interval
vectors suitable for transfer to Python pyHRV workflows. The function does
not invoke Python or pyHRV.
}
\details{
Input rows are retained in an auditable interval table. Missing,
non-positive, implausible, and repeated intervals are flagged explicitly
rather than removed silently.

Automatic unit assessment first examines the interval-column name. Names
containing common millisecond markers are interpreted as milliseconds.
Otherwise, the median positive interval is used: values no greater than 10
are interpreted as seconds, and values at least 100 as milliseconds.
Intermediate values are considered ambiguous and require an explicit unit.

CSV interval files contain one numeric millisecond value per line, without
row names, quotation marks, or a header. They can therefore be read into
Python as a one-dimensional numeric vector.

Repeated-interval collapsing is optional because physiologically genuine
adjacent intervals may occasionally have identical values. The setting
should be chosen according to the structure of the source export.
}
\examples{
ibi <- data.frame(
  participant = c("P01", "P01", "P01"),
  IBI_clean_ms = c(800, 810, 790)
)

prepared <- prepare_gazepoint_pyhrv_input(
  ibi,
  group_cols = "participant"
)

prepared$vectors$P01
prepared$manifest

}
\seealso{
\code{\link[=run_gazepoint_pyhrv_style]{run_gazepoint_pyhrv_style()}},
\code{\link[=check_gazepoint_pyhrv_interval]{check_gazepoint_pyhrv_interval()}},
\code{\link[=prepare_gazepoint_rhrv_input]{prepare_gazepoint_rhrv_input()}}
}
