% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pupil-gaze-cleaning.R
\name{filter_gazepoint_gaze}
\alias{filter_gazepoint_gaze}
\title{Filter implausible Gazepoint gaze samples}
\usage{
filter_gazepoint_gaze(
  data,
  x_col = NULL,
  y_col = NULL,
  time_col = NULL,
  group_cols = NULL,
  screen_bounds = c(0, 1, 0, 1),
  max_velocity = Inf,
  drop_invalid = FALSE,
  suffix = "_filtered"
)
}
\arguments{
\item{data}{Gaze data frame.}

\item{x_col}{Gaze x column. If NULL, common Gazepoint columns are detected.}

\item{y_col}{Gaze y column. If NULL, common Gazepoint columns are detected.}

\item{time_col}{Optional time column.}

\item{group_cols}{Optional grouping columns.}

\item{screen_bounds}{Numeric vector \code{c(x_min, x_max, y_min, y_max)}.}

\item{max_velocity}{Maximum allowed gaze velocity in coordinate units per
second. Use \code{Inf} to disable velocity filtering.}

\item{drop_invalid}{If TRUE, return only valid rows.}

\item{suffix}{Suffix for filtered coordinate columns.}
}
\value{
Data frame with validity flags, velocity, filter reason, and filtered
x/y columns.
}
\description{
Flags and optionally removes gaze samples outside screen bounds or with
implausibly high point-to-point velocity.
}
\examples{
gaze <- data.frame(time_s = 1:3, BPOGX = c(.1, .2, 2), BPOGY = c(.2, .3, .4))
filter_gazepoint_gaze(gaze, screen_bounds = c(0, 1, 0, 1))
}
