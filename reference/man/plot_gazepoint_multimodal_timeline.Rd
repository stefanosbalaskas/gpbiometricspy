% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multimodal-timeline-plot.R
\name{plot_gazepoint_multimodal_timeline}
\alias{plot_gazepoint_multimodal_timeline}
\title{Plot multimodal Gazepoint biometric timelines}
\usage{
plot_gazepoint_multimodal_timeline(
  data,
  time_col = NULL,
  signal_cols = NULL,
  group_cols = NULL,
  participant_col = NULL,
  stimulus_col = NULL,
  trial_col = NULL,
  event_time_col = NULL,
  event_col = NULL,
  standardise = TRUE,
  show_event_markers = TRUE,
  title = NULL
)
}
\arguments{
\item{data}{A data frame containing biometric samples or aligned biometric
rows.}

\item{time_col}{Optional time column. If \code{NULL}, common time columns are
detected automatically.}

\item{signal_cols}{Optional biometric signal columns. If \code{NULL}, common
Gazepoint biometric columns are detected automatically.}

\item{group_cols}{Optional grouping columns used to separate trajectories.}

\item{participant_col, stimulus_col, trial_col}{Optional common grouping columns
to add to \code{group_cols}.}

\item{event_time_col}{Optional column containing event times for vertical
markers.}

\item{event_col}{Optional event/TTL indicator column used for vertical markers.}

\item{standardise}{Logical. If \code{TRUE}, signals are z-scored within channel for
visual comparison.}

\item{show_event_markers}{Logical. Should event markers be drawn when
available?}

\item{title}{Optional plot title.}
}
\value{
A ggplot object with the long plotting data stored in the \code{plot_data}
attribute and settings stored in the \code{settings} attribute.
}
\description{
Creates a conservative timeline plot for one or more biometric channels. The
plot is intended for inspection, synchronization checks, and reporting
support. It does not interpret electrodermal activity as emotional valence.
}
