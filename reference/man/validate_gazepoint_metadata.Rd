% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pupil-qc-reporting.R
\name{validate_gazepoint_metadata}
\alias{validate_gazepoint_metadata}
\title{Validate Gazepoint metadata and required columns}
\usage{
validate_gazepoint_metadata(
  data,
  required_cols = character(),
  expected_cols = character(),
  id_cols = NULL,
  time_col = NULL,
  unique_cols = NULL,
  allow_missing_ids = FALSE
)
}
\arguments{
\item{data}{A data frame.}

\item{required_cols}{Character vector of required columns.}

\item{expected_cols}{Optional character vector of expected but non-fatal
columns.}

\item{id_cols}{Optional character vector naming participant/session/trial
identifiers.}

\item{time_col}{Optional time column used for ordering checks.}

\item{unique_cols}{Optional character vector defining a row-level key that
should be unique.}

\item{allow_missing_ids}{Logical. If \code{FALSE}, missing values in \code{id_cols} are
reported as problems.}
}
\value{
A list containing status, problems, warnings, and a summary table. The
object has class \code{"gazepoint_metadata_validation"}.
}
\description{
Performs transparent metadata checks for Gazepoint workflow data frames. The
function checks required columns, optional expected columns, missing IDs,
duplicate key rows, and time ordering within groups. It returns a structured
report rather than modifying the data.
}
\examples{
d <- data.frame(
  participant = c("P01", "P01"),
  time = c(1, 2),
  pupil_left = c(3.1, 3.2)
)
validate_gazepoint_metadata(
  d,
  required_cols = c("participant", "time"),
  id_cols = "participant",
  time_col = "time"
)
}
