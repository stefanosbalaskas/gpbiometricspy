% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aoi-assignment.R
\name{assign_gazepoint_aoi}
\alias{assign_gazepoint_aoi}
\title{Assign Gazepoint coordinates to areas of interest}
\usage{
assign_gazepoint_aoi(
  data,
  aois,
  x_col = NULL,
  y_col = NULL,
  aoi_label_col = "aoi",
  format = c("auto", "rectangle", "polygon"),
  aoi_id_col = NULL,
  data_match_cols = NULL,
  aoi_match_cols = data_match_cols,
  xmin_col = "xmin",
  xmax_col = "xmax",
  ymin_col = "ymin",
  ymax_col = "ymax",
  vertex_x_col = "vertex_x",
  vertex_y_col = "vertex_y",
  priority_col = NULL,
  overlap = c("priority", "first", "smallest", "all", "error"),
  boundary = c("inside", "outside"),
  output_col = "AOI",
  match_count_col = "aoi_match_count",
  ambiguous_col = "aoi_ambiguous",
  status_col = "aoi_assignment_status",
  all_separator = "|",
  overwrite = FALSE
)
}
\arguments{
\item{data}{A data frame containing gaze coordinates or fixation centroids.}

\item{aois}{A data frame containing rectangular or polygonal AOI definitions.}

\item{x_col}{Numeric horizontal-coordinate column in \code{data}. If \code{NULL},
common Gazepoint and fixation-summary names are searched.}

\item{y_col}{Numeric vertical-coordinate column in \code{data}. If \code{NULL},
common Gazepoint and fixation-summary names are searched.}

\item{aoi_label_col}{AOI label column in \code{aois}.}

\item{format}{AOI-definition format: \code{"auto"}, \code{"rectangle"}, or
\code{"polygon"}.}

\item{aoi_id_col}{Optional AOI identifier column. For polygon definitions,
this distinguishes multiple polygons with the same label.}

\item{data_match_cols}{Optional columns in \code{data} used to restrict eligible
AOI definitions, for example \code{"trial"} or \code{"MEDIA_ID"}.}

\item{aoi_match_cols}{Corresponding columns in \code{aois}. Must have the same
length and order as \code{data_match_cols}.}

\item{xmin_col, xmax_col, ymin_col, ymax_col}{Rectangle-boundary columns.}

\item{vertex_x_col, vertex_y_col}{Polygon-vertex columns.}

\item{priority_col}{Optional numeric priority column. Smaller values receive
higher priority when \code{overlap = "priority"}.}

\item{overlap}{Rule for coordinates falling within multiple AOIs:
\code{"priority"}, \code{"first"}, \code{"smallest"}, \code{"all"}, or \code{"error"}.}

\item{boundary}{Should coordinates on AOI boundaries be treated as
\code{"inside"} or \code{"outside"}?}

\item{output_col}{Name of the generated AOI-label column.}

\item{match_count_col}{Name of the generated AOI-match-count column.}

\item{ambiguous_col}{Name of the generated logical ambiguity column.}

\item{status_col}{Name of the generated assignment-status column.}

\item{all_separator}{Separator used when \code{overlap = "all"}.}

\item{overwrite}{Logical. If \code{FALSE}, existing output columns are protected.}
}
\value{
A data frame with class \code{"gazepoint_aoi_assignment"}. Attributes
\code{aoi_assignment_log}, \code{aoi_assignment_settings}, and \code{aoi_definitions}
contain structured provenance information.
}
\description{
Assigns sample-level gaze coordinates or fixation centroids to rectangular
or polygonal areas of interest (AOIs). The original rows are preserved and
augmented with an AOI label, match count, ambiguity flag, and assignment
status.
}
\details{
Rectangular AOIs use one row per definition with minimum and maximum x and y
coordinates. Polygon AOIs use long-format vertices, with one row per vertex
and repeated AOI identifiers or labels.

Optional matching columns can restrict AOIs to particular trials, screens,
media items, stimuli, or other recording contexts. Missing or blank values
in AOI matching columns act as wildcards.

Supported assignment statuses are:
\itemize{
\item \code{"matched"}: exactly one AOI matched;
\item \code{"ambiguous_resolved"}: multiple AOIs matched and one was selected;
\item \code{"ambiguous_all"}: all matching labels were retained;
\item \code{"unmatched"}: valid coordinates did not fall within an eligible AOI;
\item \code{"invalid_coordinate"}: x or y was missing or non-finite.
}

Coordinate systems are not transformed. Gaze coordinates and AOI
definitions must therefore use the same scale, orientation, and origin.
}
\examples{
gaze <- data.frame(
  gaze_x = c(0.1, 0.5, 0.9),
  gaze_y = c(0.5, 0.5, 0.5)
)

rectangles <- data.frame(
  aoi = c("left", "right"),
  xmin = c(0, 0.6),
  xmax = c(0.4, 1),
  ymin = c(0, 0),
  ymax = c(1, 1)
)

assign_gazepoint_aoi(
  gaze,
  rectangles,
  x_col = "gaze_x",
  y_col = "gaze_y"
)

}
\seealso{
\code{\link[=summarize_gazepoint_fixations]{summarize_gazepoint_fixations()}},
\code{\link[=summarize_gazepoint_aoi_dwell]{summarize_gazepoint_aoi_dwell()}},
\code{\link[=summarise_gazepoint_aoi_biometrics]{summarise_gazepoint_aoi_biometrics()}}
}
