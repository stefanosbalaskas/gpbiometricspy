% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-bundle-readiness.R
\name{run_gazepoint_biometrics_real_data_readiness}
\alias{run_gazepoint_biometrics_real_data_readiness}
\title{Run a final real-data readiness gate for Gazepoint biometrics data}
\usage{
run_gazepoint_biometrics_real_data_readiness(
  data = NULL,
  workflow_result = NULL,
  min_rows = 100,
  min_active_signal_count = 1,
  max_missing_prop = 0.5,
  required_signal_cols = NULL,
  require_gsr_us_preferred = TRUE,
  require_ibi_for_hrv = FALSE,
  time_col = NULL,
  ttl_cols = NULL
)
}
\arguments{
\item{data}{A biometric data frame. If \code{NULL}, the function tries to extract a
data frame from \code{workflow_result}.}

\item{workflow_result}{Optional workflow/list object containing biometric data.}

\item{min_rows}{Minimum number of rows expected for a usable real-data check.}

\item{min_active_signal_count}{Minimum number of biometric signal columns with
at least one non-missing/non-zero value.}

\item{max_missing_prop}{Maximum acceptable missing proportion for detected
signal columns before a warning is raised.}

\item{required_signal_cols}{Optional signal columns that must be present.}

\item{require_gsr_us_preferred}{Logical. If \code{TRUE}, warns when \code{GSR_US} is
absent but \code{GSR} is present.}

\item{require_ibi_for_hrv}{Logical. If \code{TRUE}, fails when HRV is present but
IBI is absent. If \code{FALSE}, this condition is reported as a warning.}

\item{time_col}{Optional time column. If \code{NULL}, common time columns are
detected automatically.}

\item{ttl_cols}{Optional TTL marker columns. If \code{NULL}, \code{ttl_marker} or
\code{TTL0}-\code{TTL6} are detected automatically.}
}
\value{
A list with \code{overview}, \code{checks}, \code{signal_summary}, and \code{settings}.
}
\description{
Provides conservative pass/warn/fail checks before using real Gazepoint
Biometrics exports for analysis or reporting. The function checks basic row
count, signal availability, missingness, time ordering, TTL availability, and
HRV/IBI caution status. It does not certify data quality or infer emotional
states.
}
